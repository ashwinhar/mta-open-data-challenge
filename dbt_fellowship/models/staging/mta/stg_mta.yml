version: 2

models:
  - name: stg_stations
    columns:
      - name: "gtfs_stop_id"
        tests:
          - unique
          - not_null
      - name: "station_id"
        tests:
          - not_null
      - name: "complex_id"
        tests:
          - not_null